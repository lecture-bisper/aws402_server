<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="bitc.aws402.board2.mapper.FileMapper">

<!--    첨부 파일 등록 -->
    <insert id="insertFile" parameterType="bitc.aws402.board2.dto.FileDTO">
        INSERT INTO t_file (board_idx, original_file_name, stored_file_name, file_size, create_date)
        VALUES (#{boardIdx}, #{originalFileName}, #{storedFileName}, #{fileSize}, NOW())
    </insert>

<!--    첨부 파일 목록 -->
    <select id="selectFileList" parameterType="int" resultType="bitc.aws402.board2.dto.FileDTO">
        SELECT file_idx, board_idx, original_file_name, file_size
        FROM t_file
        WHERE board_idx = #{boardIdx}
        ORDER BY file_idx
    </select>

<!--    첨부 파일 상세 -->
    <select id="selectFileDetail" parameterType="int" resultType="bitc.aws402.board2.dto.FileDTO">
        SELECT file_idx, board_idx, original_file_name, stored_file_name, file_size, create_date
        FROM t_file
        WHERE file_idx = #{fileIdx}
    </select>
</mapper>
