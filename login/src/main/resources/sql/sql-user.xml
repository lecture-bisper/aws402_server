<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="bitc.aws402.login.mapper.UserMapper">
    <select id="isLogin" parameterType="map" resultType="int">
        SELECT COUNT(*) AS cnt FROM t_user
        WHERE user_id = #{userId}
        AND user_pw = #{userPw}
    </select>

    <select id="selectUserInfo" parameterType="String" resultType="UserDTO">
        SELECT user_no, user_id, user_name FROM t_user WHERE user_id = #{userId}
    </select>
</mapper>
