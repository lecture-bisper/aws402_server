<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="bitc.aws402.board4.mapper.BoardMapper">

<!--    게시물 목록 -->
    <select id="selectBoardList" resultType="BoardDTO">
        SELECT board_idx, board_title, board_create_id, board_create_date, board_hit_cnt
        FROM t_board
        WHERE deleted_yn = 'N'
        ORDER BY board_idx DESC
    </select>

<!--    게시물 상세 -->
    <select id="selectBoardDetail" parameterType="int" resultType="BoardDTO">
        SELECT board_idx, board_title, board_contents, board_create_id, board_create_date, board_update_id, board_update_date, board_hit_cnt
        FROM t_board
        WHERE deleted_yn = 'N'
        AND board_idx = #{boardIdx}
    </select>

<!--    게시물 조회수 증가 -->
    <update id="updateHitCnt" parameterType="int">
        UPDATE t_board
        SET board_hit_cnt = board_hit_cnt + 1
        WHERE board_idx = #{boardIdx}
    </update>

<!--    게시물 등록 -->
    <insert id="insertBoard" parameterType="BoardDTO">
        INSERT INTO t_board (board_title, board_contents, board_create_id, board_create_date)
        VALUES (#{boardTitle}, #{boardContents}, #{boardCreateId}, NOW());
    </insert>

<!--    게시물 수정 -->
    <update id="updateBoard" parameterType="BoardDTO">
        UPDATE t_board
        SET board_title = #{boardTitle}, board_contents = #{boardContents}, board_update_id = #{boardUpdateId}, board_update_date = NOW()
        WHERE board_idx = #{boardIdx};
    </update>

<!--    게시물 삭제 -->
    <update id="deleteBoard" parameterType="int">
        UPDATE t_board
        SET deleted_yn = 'Y'
        WHERE board_idx = #{boardIdx};
    </update>

</mapper>
